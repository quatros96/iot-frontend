import { SensorIconPipe } from './sensor-icon.pipe';

describe('SensorIconPipe', () => {
  it('create an instance', () => {
    const pipe = new SensorIconPipe();
    expect(pipe).toBeTruthy();
  });
});
